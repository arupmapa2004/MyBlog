<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Articles</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
<style>
:root {
    --bg: #f0f4f8;
    --card-bg: #fff;
    --accent: #4f46e5;
    --accent-hover: #4338ca;
    --text: #111827;
    --muted: #6b7280;
    --radius: 16px;
    --shadow: 0 20px 40px rgba(0,0,0,0.08);
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
    font-family: 'Inter', sans-serif;
    background: var(--bg);
    color: var(--text);
}

header {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
}

header h1 {
    font-size: 1.8rem;
    color: var(--accent);
}

header nav a {
    margin-left: 20px;
    text-decoration: none;
    color: var(--text);
    font-weight: 600;
    transition: color 0.3s ease;
}

header nav a:hover {
    color: var(--accent);
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 32px 16px;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 24px;
}

.card {
    background: var(--card-bg);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
    transform: translateY(-6px);
    box-shadow: 0 25px 50px rgba(0,0,0,0.15);
}

.card img {
    width: 100%;
    height: 200px;
    object-fit: cover;
}

.card-content {
    padding: 20px;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
}

.card h3 {
    margin: 0 0 10px 0;
    font-size: 1.25rem;
    font-weight: 600;
}

.card h3 a {
    text-decoration: none;
    color: var(--text);
    transition: color 0.3s ease;
}

.card h3 a:hover {
    color: var(--accent);
}

.card p {
    flex-grow: 1;
    color: #4b5563;
    font-size: 0.95rem;
    margin-bottom: 16px;
}

.card-meta {
    display: flex;
    justify-content: space-between;
    font-size: 0.85rem;
    color: var(--muted);
    margin-bottom: 12px;
}

.card-meta span:first-child::before { content: "📅 "; }
.card-meta span:last-child::before { content: "✍️ "; }

.read-more {
    align-self: flex-start;
    padding: 8px 20px;
    border-radius: 25px;
    background: var(--accent);
    color: white;
    font-weight: 600;
    text-decoration: none;
    transition: background 0.3s ease, transform 0.2s ease;
}

.read-more:hover {
    background: var(--accent-hover);
    transform: translateY(-2px);
}

@media(max-width: 480px){
    .container { grid-template-columns: 1fr; padding: 16px; }
    .card img { height: 180px; }
    header { flex-direction: column; align-items: flex-start; gap: 12px; }
}
</style>
</head>
<body>

<header>
    <h1>Create Your Blog</h1>
    <nav>
        <a href="/">Home</a>
        <a href="/login">Login</a>
        <a href="/contactus">Contact Us</a>
    </nav>
</header>

<div class="container">
    <% articles.forEach(article => { %>
    <div class="card">
        <img src="/images/<%= article.image %>" alt="<%= article.title %>">
        <div class="card-content">
            <h3><a href="/post/<%= article.articleId %>"><%= article.title %></a></h3>
            <p><%- article.content.slice(0, 100) %>...</p>
            <div class="card-meta">
                <span class="created" data-date="<%= article.created_at %>"></span>
                <span><%= article.user_username %></span>
            </div>
            <a href="/post/<%= article.articleId %>" class="read-more">Read More</a>
        </div>
    </div>
    <% }) %>
</div>

<script>
document.querySelectorAll('span.created').forEach(span => {
    const date = new Date(span.dataset.date);
    const dayName = new Intl.DateTimeFormat('en-US', { weekday: 'long' }).format(date);
    const day = String(date.getDate()).padStart(2,'0');
    const month = String(date.getMonth()+1).padStart(2,'0');
    const year = date.getFullYear();
    span.textContent = `${day}-${month}-${year} (${dayName})`;
});
</script>

</body>
</html>
